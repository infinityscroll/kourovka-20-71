# Independent verification (2026-07-21)

Separately from the discovery pipeline, the witness was re-verified from the
raw edge list alone with fresh code (no shared components with the search or
verifier scripts in this repository):

- automorphism group by brute force over all 9! = 362,880 permutations:
  order 2, generated by (0 5)(1 4)(2 3)(6 8);
- vertex orbits: {0,5}, {1,4}, {2,3}, {6,8}, {7} — five orbits;
- card classes by pairwise `networkx.is_isomorphic` on all 9 vertex-deleted
  subgraphs: {0,5}, {1,4}, {2,3,6,8}, {7} — four classes;
- connectivity and graph6 string `HCpbdgy` confirmed.

The Lean certificate `Kourovka2071.lean` compiles with zero `sorry`/`admit`/
`axiom` against Lean 4.31 / current Mathlib (`lake env lean Kourovka2071.lean`
inside a google-deepmind/formal-conjectures checkout).
